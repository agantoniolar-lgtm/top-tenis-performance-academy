# Session — 2026-07-18

## Dónde
- `CLAUDE.md` (regla 6, mobile-first)
- `COWORK_PROJECT_INSTRUCTIONS.md` (paso 2 del bloque a pegar en Cowork Settings)
- `SETUP_CHECKLIST.md` (sección 1 y sección 6)
- `.claude/skills/commit/SKILL.md` (description + "Cuándo se usa")
- `.claude/skills/build/`, `.claude/skills/design/`, `.claude/skills/verify-tests/`, `.claude/skills/verify-evals/`, `.claude/skills/verify-ui/` (empaquetados como skills de Cowork a nivel cuenta)
- `.claude/skills/feature-build-flow/SKILL.md` y `.claude/skills/schema-rls-verification/SKILL.md` (borrados del repo)
- `TASKS.md`, `TASKS_ARCHIVE.md` (T156 movido a done, T157 nuevo creado y archivado)

## Qué se hizo
La sesión arrancó confirmando que el tracking de tasks ya no vive en Notion (migración del 2026-07-17 a `TASKS.md`), y encontrando que el bloque pegado en Cowork Settings → Project Instructions seguía describiendo el esquema viejo (Notion + skills `session-open-close`/`commit-kanban-sync`). Se corrigió ese bloque para apuntar a `TASKS.md` y a los skills vigentes (`session-log`, `commit`), y Marco lo volvió a pegar en Cowork.

De ahí surgió un problema más de fondo: el registro de skills de Cowork estaba desincronizado del repo, en las dos direcciones. `ListSkills` mostraba duplicados obsoletos (`commit-kanban-sync`, `session-open-close`) que ya no existen en `.claude/skills/`, y al mismo tiempo le faltaban cinco skills genéricos del kit que sí existen ahí (`build`, `design`, `verify-tests`, `verify-evals`, `verify-ui`) — ya escritos en inglés, con placeholders `{{...}}`, sin nada específico de Top Tennis. Refrescar la sesión movió el problema (quitó los duplicados en una pasada, pero de paso hizo desaparecer temporalmente `feature-build-flow` del registro) sin resolverlo limpio — quedó como una inconsistencia de plataforma, no de este repo.

Marco explicó la intención de fondo: migrar de skills específicos del proyecto hacia un "kit" estándar que sirva para cualquier proyecto en Cowork/Code, no solo Top Tennis — y ya había quitado `feature-build-flow` y `commit-kanban-sync` del registro de Cowork por su cuenta como parte de ese cambio. Se usó `skill-creator` (sin interview, contenido ya aprobado) para empaquetar los 5 skills genéricos con `package_skill.py`, preservando el contenido tal cual del repo, y se presentaron vía tarjeta "Save skill" — Marco los instaló.

Antes de tocar `feature-build-flow`, se notó que ya no hacía falta como orquestador: cada uno de los 5 skills nuevos ya declara en su propia descripción cuándo dispara y a quién le entrega después (`scope`→`design`→`build`→`verify-tests`/`verify-evals`/`verify-ui`→`commit`), así que el kit se auto-encadena sin necesitar un director de flujo central. La instrucción de Marco fue dejar `feature-build-flow/SKILL.md` donde estaba pero quitar las referencias activas hacia él — se editaron `CLAUDE.md`, `COWORK_PROJECT_INSTRUCTIONS.md` y `.claude/skills/commit/SKILL.md` (el intento de escribir este último vía el bridge remoto lo bloqueó una restricción de seguridad sobre la carpeta `.claude/` — Marco aplicó ese cambio manualmente desde su terminal). `SETUP_CHECKLIST.md` se actualizó para reflejar el estado nuevo.

Al cerrar, `git status` mostró dos borrados sin comitear que no estaban acordados explícitamente en la conversación: `feature-build-flow/SKILL.md` y `schema-rls-verification/SKILL.md`. Se confirmó con Marco que ambos son intencionales — generalizó `schema-rls-verification` como `verify-rls` (mismo patrón que los otros 5) por su cuenta, y decidió que `feature-build-flow` ya no hace falta ni siquiera project-local. En el barrido de cierre de `TASKS.md`, Marco confirmó que T156 (P&M v2 — separar outcome/carryover) ya se probó en vivo y se le hizo push, así que se movió a done; T086 y T102 siguen in progress sin cambios.

## Decisiones clave
- El kit de skills (scope/design/build/verify-tests/verify-evals/verify-ui/commit/session-log) se auto-orquesta sin necesitar un skill "director" — se descarta la idea de generalizar `feature-build-flow` como 6to skill del kit.
- `feature-build-flow/SKILL.md` y `schema-rls-verification/SKILL.md` se borran del repo (superseded por el kit genérico y por `verify-rls` respectivamente) — decisión explícita de Marco, confirmada al notar los borrados sin comitear.
- La inconsistencia entre el registro de `ListSkills` y el listado de skills disponibles para el Skill tool queda identificada como problema de plataforma, no de este repo — no se intentó resolver más allá de refrescar la sesión.

## Pendientes / follow-ups
- Confirmar en la próxima sesión que `build`, `design`, `verify-tests`, `verify-evals`, `verify-ui` y `verify-rls` sigan apareciendo consistentemente en el registro de skills — la desincronización de hoy podría reaparecer.
- Backlog no bloqueante, sin fecha (sin cambios esta sesión): warnings de Supabase Advisors, decisión de branch-per-task, decisión de typing (JSDoc/PropTypes vs. dejarlo como está) — ver `SETUP_CHECKLIST.md`.
- T086 (Home.jsx) y T102 (P&M close-quarterly-plan, master) siguen in progress, sin cambios esta sesión.

# Sesión 2026-07-24

## Dónde
- `docs/prd-context-tagging-engine.md` (**nuevo**, T174) — el entregable central de la sesión.
- `docs/prd-notas-voz-contexto-atleta.md` (§6 fase 3, §8 tabla de verificación) — actualizado.
- `docs/uploads/tagging-system.md` — doc de referencia de m-brain que Marco subió; se versionó al repo porque el PRD lo cita.
- `TASKS.md` — T174 creado (`in progress`); nota nueva en T148.
- `BACKLOG.md` — T175 agregado.
- Lectura de contexto (sin cambios): `supabase/functions/generate-quarterly-plan/index.ts` (`SUB_DIMENSIONS`, 21 sub-dimensiones canónicas), `src/lib/athletics.js` (constantes de taxonomía duplicadas a medias).
- Commit `5cf5db8` en `main` (docs-only; lint + 195 tests verdes, gitleaks limpio). Push lo hace Marco.

## Qué se hizo
Sesión enteramente de **scope + diseño de PRD**, sin build. Se abrió para retomar T148 (servicio de notas), cuyo siguiente paso era la **fase 3 — curación del bundle**. Al scopearla, Marco decidió que la agrupación de fase 3 la hace **el coach a mano**, apoyada por un **tagging system manual** — y que ese tagging es en realidad el cimiento de un **context & tagging engine** (RAG + manejo de contexto) para todo el sistema de IA del producto, no solo los planes. Por su alcance y por estar gated por T150 (como la fase 4 de T148), se elevó a su **propio task y PRD (T174)**, linkeado desde T148, en vez de inflar el PRD de notas.

Se sintetizó y adaptó el patrón de tagging de m-brain (`docs/uploads/tagging-system.md`) al dominio de coaching de tenis: taxonomía **facetada** de vocabulario cerrado, reusando la taxonomía canónica del plan (`SUB_DIMENSIONS`) para las facetas `dimension`/`sub_dimension`. Se rebotaron unas notas que Marco trajo de una discusión con Gemini y se afinaron en dos iteraciones más, cerrando el vocabulario v0 y las decisiones de arquitectura.

También se respondieron dos preguntas de aprendizaje de Marco: (1) a quién preguntar / qué leer para mejorar su entendimiento de tagging + context engineering (information architecture / "The Accidental Taxonomist" para el vocabulario; applied AI engineer + blog de Anthropic "Contextual Retrieval" + pgvector/Supabase + evals de Hamel Husain para la mitad de RAG); (2) validación de su supuesto de RAG (correcto no entrenar modelo; pero recuperación estructurada por tags antes que embeddings — a este volumen los embeddings aún no hacen falta).

## Decisiones clave
- **Fase 3 de T148 = tagging + agrupación MANUAL, sin LLM** → no toca `verify-evals`, no depende de T150, enviable a prod. El auto-tagging LLM se difiere a fase 4+ (gated por T150). Los tags manuales se vuelven (a) el eval dataset de T150 y (b) el índice de recuperación estructurada (RAG v0). "Ladrillo por ladrillo".
- **Engine separado en T174** (PRD propio) — es infra transversal de otra altitud que sobrevive a T148.
- **Vocabulario v0 cerrado:** facetas nota-nivel `segment` (ya existe) + `dimension` (1–3) + `sub_dimension` (0–3, jerárquica, reusa las 21 keys) + `note_type`. `player_archetype` a nivel atleta.
- **`valence` descartada** — sería nullable y se traslapaba con `note_type` sin definición clara. La polaridad va vía `note_type` y como tendencia derivada.
- **`note_type` v0:** `observacion` / `failure` / `setback` (problema dividido por agencia; **lesión = setback**, salud = default sin taggear) / `intencion` (era "accion") / `progreso` (intermedio) / `hito`. Flujo: observación/problema → intención → progreso → hito.
- **Modelo de datos = tabla nueva `note_tags`** (aislada de `athlete_notes`, sin riesgo de corrupción) — decidido, ya no "A vs B". RLS hereda de `athlete_notes`, se verificará con `verify-rls`.
- **Módulo compartido = `supabase/functions/_shared/taxonomy.ts`** (datos planos): la Edge Function importa `../_shared/`, el front (Vite) importa el mismo archivo **en build** (no fetch en runtime). Cero duplicación. Plan B si el import cross-carpeta da fricción: guard test de drift en CI.
- **Tendencias (patrones fortaleza/debilidad) = capa derivada futura**, se construye cuando ya se taggee en producción (recurrencia de failure/setback sobre una sub-dimensión a lo largo de periodos = debilidad). Ej. de Marco: "le cuesta el 2º saque en partidos por varios periodos" = tendencia.
- **Umbral de corpus para arrancar auto-tagging (T150) = 50 notas manuales.**
- **`confidence`/severidad diferido** — buena idea, sin lugar claro aún.
- **Salud/lesión NO es faceta propia.**
- **RAG:** recuperación estructurada por tags primero; embeddings (pgvector/Supabase) diferidos hasta que el filtro estructurado no alcance. El ensamblado multi-part de prompts y la inferencia outcome→arquetipo son consumidores aguas abajo, fuera del alcance del engine (cada uno su task futuro). La inferencia "qué sirve para qué tipo de jugador" = generación de hipótesis, no prueba (N=10 chico); lo accionable hoy es que el schema pueda responderla (capturar `player_archetype` + ligadura objetivo→outcome limpia).

## Pendientes / follow-ups
- **Próxima sesión: `design` + `build` del slice manual de fase 3** (T148 + T174 juntos): UI de tagging (chips/dropdowns del vocabulario cerrado) en `src/pages/portal/coach/AlumnoDetalle.jsx`, migración de tabla `note_tags` + `verify-rls` en sandbox, módulo compartido `supabase/functions/_shared/taxonomy.ts`, y la vista de curación manual. Sin bloqueos de diseño pendientes.
- **En cancha de Marco antes del build:** validar con coaches el vocabulario de `note_type` y la lista de `player_archetype` (§3.4, §3.7 del PRD del engine).
- **Preguntas abiertas del PRD (§13), todas de calibración/validación, no bloquean:** dónde meter `confidence`; validar arquetipos; topes de cardinalidad y reglas de conflicto provisionales; si la consolidación de `SUB_DIMENSIONS` entra en el slice de fase 3 o es refactor previo.
- **T148 fase 4** sigue bloqueada por T150 (convención de evals) — no cambia.
- T175 (dashboards de uso/engagement) en backlog, Low — revisar primero si Supabase trae algo nativo.
- Sin cambios en el resto: T173→T169 (matriz normativa, esperando coach), T172 (dominio Resend), T116 (CMS, pausa marketing).

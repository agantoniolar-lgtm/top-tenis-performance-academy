# Session 21 — Conexión CMS ↔ landing + limpieza de datos

Date: June 30, 2026
Key decisions: (1) El CMS se queda en modelo de slots ESTÁTICOS con deploys ad hoc — meter/quitar slots o páginas = cambio de código + deploy (Marco+Claude), no page-builder dinámico. (2) Atletas/highlights del CMS son para el portal detrás del login, NO para el sitio público (apartado para otra sesión). (3) En la limpieza se conserva agantoniolar (login de Marco) además de valerochi como content managers. (4) El Test Coach mantiene rol 'Head Coach' porque es el slot de Armando, que se ligará a su cuenta cuando la cree.
Open items / follow-ups: (1) SIGUE PENDIENTE Y SIN TRABAJAR: la cola de planning+measurement pm-v2.2 (probar prompt con dumps reales, 3 huecos de UX, validate-quarterly-plan v2, gate de buy-in de coaches) — es el foco principal de la próxima sesión. (2) Atletas/highlights en el portal: cablear foto_url + athlete_media en las vistas del portal (hoy athlete_media no se lee en ningún lado). (3) Scopear juntos el registro único de slots CMS↔landing. (4) Borrar carpeta duplicada basura top-tenis-performance-academy/top-tenis-performance-academy/. (5) commit+push de esta sesión (rm .git/index.lock primero).
Status: Complete
What we did: Diagnosticamos el content management tool: estaba write-only — el panel guardaba en content_blocks/media_assets/athlete_media pero ninguna página pública leía esas tablas, así que todo el sitio estaba hardcodeado (incluidos coaches ficticios y 3 fotos de coaches subidas que no se mostraban). Construimos la capa de lectura: src/lib/content.js (con 12 tests: youTubeId/contentKey/indexBySlot), el provider src/contexts/PublicContent.jsx (usePublicContent, un solo fetch con fallback al contenido actual) y componentes CmsVideo/CmsImage. Cableamos Home, Programas y CaminoUSA para que textos, imágenes y videos se editen desde el CMS. Nosotros ahora carga los coaches reales desde la tabla coaches: migración add_coach_profile_fields (rol/credencial/bio/orden/visible_en_sitio) + seed de Armando + editor de esos campos en la pestaña Perfiles del panel (antes los coaches del sitio eran ficticios y no coincidían con la BD). Arreglamos dos bugs: el reemplazo de imágenes no refrescaba por cache (cache-busting ?v= en fotos de perfil/highlight) y el slot de imagen de Programas estaba mal etiquetado (era la sede, no hero); además agregamos un slot real de imagen de hero a Programas. Todo verificado con lint + 65 tests + build (el dist del sandbox es solo-lectura, se compiló a temporal). Cerramos con un reset de datos: la BD quedó con Test Athlete (ex-Marco Damián, con sus 5 reportes/13 torneos/plan intactos), Test Coach con rol Head Coach (ex-Armando, ligado a mailto:tlaca@toptenis.mx) y dos content managers (valerochi + agantoniolar); se borraron 3 atletas dummy (Emiliano/Ian/Ricardo), 3 coaches (Jesús/Lalo/Miguel), el CM marcotest y 3 usuarios auth, conservando las 9 imágenes de landing.
# Session — Design Polish P1–P4 + DB fixes · 22 Jun 2026

Date: June 22, 2026
Key decisions: Email confirmation queda deshabilitado para el MVP — el flow de signup actual no soporta el INSERT del atleta con confirmation habilitada. Armando Tlacaelel es el único registro de ese coach, ligado a mailto:tlaca@toptenis.mx. Content management para la persona de diseño/marketing: recomendación es admin panel en la plataforma + Supabase Storage (sin servicios externos).
Open items / follow-ups: Marco debe deshabilitar email confirmation en Supabase (Authentication → Sign In / Providers → Email) antes de volver a testear el signup de atletas. Commit y push pendientes de toda la sesión: P1-P3 design polish, Dashboard fix (Desconocido → /login), Signup fix (signOut on INSERT failure). Próxima sesión: landing page + admin panel de content management para persona de marketing.
Status: Complete
What we did: Sesión de final polish basada en el review de Claude Design. Se implementaron los cuatro Ps: P1 rediseñó ReportesPorPeriodo con el componente PendingStrip (muestra atletas sin reporte y AV pendiente por nombre), period picker con chip ACTUAL y flecha derecha deshabilitada en el período actual, bordes izquierdos de colores en cada fila de la tabla, y botón '+  Crear reporte'. P2 rediseñó la pantalla de onboarding del atleta con el componente OnboardingCard (número grande, barra de progreso, estado bloqueado para paso 2 si perfil incompleto) y una propuesta de valor en el header. P3 alineó todas las pantallas de auth (Login, Signup, CoachSignup, RegistroPendiente) al design system: inputs copper on focus, botones uppercase con Bricolage, panel izquierdo verde oscuro, help texts explicativos para el coach dropdown y el código de invitación. P4 (sidebar nav — quitar Nuevo reporte) ya estaba implementado desde P1. En paralelo, se resolvieron bugs de DB: se eliminó el coach duplicado 'Tlacaelel Coach' y se reasignó todo (4 atletas, 5 reportes, report_on_court, report_physical, report_character) a Armando Tlacaelel, ahora ligado a mailto:tlaca@toptenis.mx. Se limpiaron múltiples registros de prueba de Antonio mailto:Aguilar/agantoniolar@gmail.com. Se diagnosticó el bug principal del signup de atletas: email confirmation está habilitado en Supabase, por lo que signUp devuelve session=null y el INSERT del atleta nunca se ejecuta. Fix pendiente: Marco debe deshabilitar email confirmation en Auth → Sign In / Providers. Se arreglaron dos bugs colaterales: Dashboard enviaba a Desconocido users en loop infinito (ahora redirige a /login), y Signup ahora hace signOut si el INSERT falla para no dejar sesiones rotas. Se creó task en backlog para revisar MFA cuando corresponda.